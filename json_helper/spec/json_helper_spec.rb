require "spec_helper"

describe JsonHelper do
  it "has a version number" do
    expect(JsonHelper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
